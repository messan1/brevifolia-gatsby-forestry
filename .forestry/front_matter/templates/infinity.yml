---
label: infinity
hide_body: false
fields:
- name: titre
  type: text
  config:
    required: false
  label: titre
- name: content
  type: blocks
  label: content
  template_types:
  - blog-post
  config:
    min: 
    max: 
- name: menu
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: infinity
  label: menu
pages:
- content/pages/infinity.md
